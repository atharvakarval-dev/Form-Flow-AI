import { RegistrationForm } from '@/features/auth';

/**
 * RegisterPage - Route wrapper for registration functionality
 */
const RegisterPage = () => {
    return <RegistrationForm />;
};

export default RegisterPage;
